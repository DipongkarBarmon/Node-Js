<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body{
      font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
  </style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>
   <h2>URL Shortener</h2>

    <% if (locals.id) { %>
      <p>URL Generate: http://localhost:8001/<%= id %></p>
    <% } %>
    
   <div>
     <form method="post" action="/url">
       <label>Enter Your Orginal URL</label>
       <input type="text" name="url" placeholder="http://example.com">
       <button type="submitt">Generate</button>
     </form>
   </div>
   <br><br><br>
   <div>
     <% if (locals.id) { %>
        <table border="collaps">
        <thead>
          <th>Serial No</th>
          <th>ShortID</th>
          <th>Redirect</th>
          <th>Clicks</th>
        </thead>
        <tbody>
           <% urls.forEach((url,index) => { %>
               <tr>
                 <td><%= index + 1%></td>
                 <td><%= url.shortId %></td>
                 <td><%= url.redirectURL %></td>
                 <td><%= url.visitHistry.length %></td>
               </tr>
           <% }) %>
        </tbody>
     </table>
     <% } %>
     
   </div>
</body>
</html>